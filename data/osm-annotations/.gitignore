# Generated by scripts/osm-annotations/fetch-osm-annotations.mjs.
# These datasets are large, regenerable, and change whenever the script is
# re-run against live OSM data — keep them out of version control the same
# way build output (dist/, dist-ssr/) already is at the repo root.
#
# This file only affects this directory; the repo's root .gitignore is
# untouched.
raw/
*.json
!.gitkeep
